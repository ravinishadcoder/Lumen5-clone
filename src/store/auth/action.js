import axios from 'axios';
import * as type from './type';


export const loginAPI = (data) => (dispatch) => {
    dispatch({type: type.LOGIN_LOADING});
     //check if email and pass is maching or not
     
    
}


